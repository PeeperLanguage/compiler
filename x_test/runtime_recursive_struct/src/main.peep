struct Node {
    value: i32,
    next: ?*Node
}

fn main() -> i32 {
    let node = alloc(.Node{
        value = 42,
        next = none
    });
    let result = node.value;
    free(node);
    return result - 42;
}
