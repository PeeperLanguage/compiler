struct Box {
    value: i32
}

    fn (self: &Box) Reference() -> &Box {
        return self;
    }

fn Leak() -> &Box {
    let box: Box = .{ value = 1 };
    return box.Reference();
}

#[extern]
fn ExternalLeak() -> &Box;

    #[extern]
    fn (self: &Box) ExternalReference() -> &Box;
