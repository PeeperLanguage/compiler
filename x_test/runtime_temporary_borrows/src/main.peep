fn Sum(values: &[]i32) -> i32 {
    return values[0] + values[1];
}

fn SetFirst(values: &mut []i32) {
    values[0] = 7;
}

fn main() -> i32 {
    let sum = Sum(&[]i32{2, 3});
    SetFirst(&mut []i32{1, 2});
    if sum == 5 {
        return 0;
    }
    return 1;
}
