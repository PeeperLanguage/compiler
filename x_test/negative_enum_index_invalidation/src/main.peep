enum Choice {
    Left: { value: i32 },
    Right: { value: i32 },
}

fn Read(values: [2]Choice, mut index: usize) -> i32 {
    if values[index] is Choice::Left {
        index = 1;
        return values[index].value;
    }
    return 0;
}
