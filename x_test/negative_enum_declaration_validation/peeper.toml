name = "negative_enum_declaration_validation"
build = "lib"

[test]
mode = "check"
outcome = "failure"
stderr_contains = ["T0021", "variant name must be PascalCase", "variant field `value` already declared", "method `inlineValue` conflicts with enum variant data field", "method `namedValue` conflicts with enum variant data field", "method `aliasedValue` conflicts with enum variant data field"]
