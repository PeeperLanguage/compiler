name = "negative_transformed_generic_recursion"
build = "lib"

[test]
mode = "check"
outcome = "failure"
stderr_contains = ["T0021", "recursive generic applications must preserve exact type arguments"]
