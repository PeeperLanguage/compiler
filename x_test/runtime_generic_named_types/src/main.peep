struct Box<T> {
    value: T
}

type Maybe<T> = ?T;

fn main() -> i32 {
    let inner: Box<i32> = .{ value = 42 };
    let outer: Box<Box<i32>> = .{ value = inner };
    let maybe: Maybe<i32> = outer.value.value;
    if maybe != none {
        return maybe;
    }
    return 1;
}
