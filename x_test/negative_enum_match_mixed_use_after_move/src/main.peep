enum Resource {
    Owned: { value: *i32 },
    Pending
}

fn Consume(_: Resource) {}

fn Invalid(resource: Resource) {
    match resource {
        Resource::Owned with { value = value } => {
            free(value);
        }
        Resource::Pending => {}
    }
    Consume(resource);
}
