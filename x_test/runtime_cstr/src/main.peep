fn sayhi() {
    let hi: cstr = "Hello, world!\n";
    write(stdout, hi, 14);
}

struct Point {
    x: i32,
    y: i32
}

fn main() -> i32 {
    let msg: cstr = "Hello from Peeper runtime ABI!\n";
    write(stdout, msg, 31);
    sayhi();
    return 0;
}
